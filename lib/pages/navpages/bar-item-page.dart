import 'package:flutter/material.dart';

class BarItempage extends StatelessWidget {
  const BarItempage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('BarItem-Page')),
    );
  }
}
